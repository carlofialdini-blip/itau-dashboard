# CLAUDE.md — Portfolio Intelligence Dashboard

Persistent project memory. Read this first in any new session before making suggestions or changing code. If it looks stale against the actual repo, reconcile it before proceeding.

---

## 1. Project Overview

- **Name:** Portfolio Intelligence Dashboard (internal working name; header reads "Portfolio Intelligence / Market Monitor")
- **Owner:** Carlo Fialdini (vittorio.fialdini@gmail.com), works at Itaú
- **Purpose:** Aggregate news, market data, and economic indicators for a tracked portfolio of Brazilian/global companies, plus dedicated Brazil, China, and Credit-market views. Built for internal team distribution — not a live web app.
- **Distribution model:** Generated **twice a day** into a single static `dashboard.html` file, then uploaded to SharePoint / sent directly to teammates. Recipients just open the file in a browser — no server, no install, no network calls at view time. This shaped almost every architectural decision below.
- **Tech stack:** Python 3 (stdlib + a handful of libs, see §5), Jinja2 templating, vanilla JS, Plotly.js (via CDN in the template). No frontend build step, no npm/Node runtime dependency for the app itself, no database.
- **Package manager:** pip, `requirements.txt` at repo root.
- **Repo:** `git@github.com:carlofialdini-blip/itau-dashboard.git` (GitHub account `carlofialdini-blip`, distinct from `carlofialdini` — SSH key is tied to the `-blip` account).

---

## 2. Architecture

### Folder structure
```
update_dashboard.py       # ENTRY POINT — run twice a day, refreshes everything
setup.py                  # ENTRY POINT — one-time per-machine bootstrap (pip install + first run)
requirements.txt
dashboard.html             # generated output (gitignored)
distribuicao-combustiveis-completo/  # separate teammate project (own nested git repo), studied for
                                      # its Chart→PPTX export architecture — see §4. Not otherwise
                                      # integrated with this project's build; do not touch its files.
assets/
  itau_logo.png            # 120x120 processed logo, embedded as base64 into dashboard.html
Itaú.jpeg                  # original source logo (3840x3840), kept for future reprocessing
data/                      # generated caches (gitignored except portfolio.xlsx)
  portfolio.xlsx           # source list of tracked companies — columns: Company, Sector, Keywords, Ticker (optional, blank-safe)
  news_cache.json           china_news_cache.json
  brazil_news_cache.json    credit_news_cache.json
  gdelt_news_cache.json     # portfolio companies, GDELT-sourced (complements news_cache.json)
  events.json               china_events.json
  brazil_events.json
  br_ettj_history.json      # accumulating Brazil 10Y yield history (ANBIMA), grows one point/day
templates/
  dashboard_template.html   # single Jinja2 template for the entire static site (5 pages)
core/
  generate_dashboard.py     # main builder: reads caches, fetches live chart data, renders template
  net_utils.py               # shared get_with_retry() HTTP helper (retry + backoff)
  scoring.py                  # shared importance_bucket() (Low/Medium/High)
  trusted_sources.py           # shared TRUSTED_DOMAINS/TRUSTED_DOMAINS_EN whitelists + is_trusted()/outlet_name()
scrapers/                   # news scrapers, one per source (not per page — see below)
  scraper.py                 # portfolio companies (from portfolio.xlsx), Google News RSS
  gdelt_scraper.py            # portfolio companies, GDELT DOC 2.0 API — PT+EN, complements scraper.py
  brazil_scraper.py          china_scraper.py          credit_scraper.py     # Google News RSS
events/                     # event-calendar generators, one per source
  events_generator.py        # earnings (yfinance) + COPOM (hardcoded) + IPCA/IBGE (API)
  brazil_events_generator.py # IBGE calendar + COPOM + BCB Focus Report (weekly Mondays)
  china_events_generator.py  # NBS/PBOC release dates (pattern-computed, no network) + conferences (hardcoded)
```

### Data flow (what `update_dashboard.py` runs, in order)
1. **Scrapers** (`scrapers/*.py`) — query Google News RSS (or, for `gdelt_scraper.py`, the GDELT DOC 2.0 API) per sector/company, score relevance, merge with existing cache → write `data/*_cache.json`. **Freshness policy differs by source**: `brazil_scraper.py` is strict same-day-only (Brasília calendar date), with per-keyword querying (not one OR-mega-query — see §4) and score-based backfill to a 30-article-per-sector floor; `scraper.py`/`china_scraper.py`/`credit_scraper.py` keep a 7-day day-balanced window (today/yesterday/older fallback buckets); `gdelt_scraper.py` keeps a 7-day window too but has no day-balancing (GDELT's own `sort:datedesc` already gives recency-first results, no relevance-ranking cap to correct for).
2. **Event generators** (`events/*.py`) — build forward-looking event calendars (mix of live APIs and hardcoded annual schedules) → write `data/*_events.json`.
3. **`core/generate_dashboard.py`** — reads all cache files + `portfolio.xlsx`, fetches *live* chart data (BCB OLINDA API for Brazil/Credit macro series, chinadata.live API for China datasets, yfinance + BCB + ANBIMA for the Cockpit market overview), **merges all 4 news sources and all 3 event sources into two unified feeds** (`load_unified_news()`, `load_unified_events()` — see Architecture below), renders `templates/dashboard_template.html` via Jinja2 → `dashboard.html`. Every chart's data is embedded inline as JSON in a `<script>` block — the file has zero `fetch()`/XHR calls, so it opens correctly via `file://` with no server.
4. **`update_dashboard.py`** orchestrates 1–3 via `subprocess`, **continues past individual step failures** (a network hiccup in one scraper doesn't block the rebuild — that source just keeps its last-cached data), 600s timeout per step.
5. **`setup.py`** = `pip install -r requirements.txt` then delegates to `update_dashboard.py`. Run once per machine (Carlo's Mac, the work Windows PC) — never sent to teammates.

### Pages in `dashboard.html`
As of the July 2026 reorganization, the dashboard is organized by **content type**, not by source — one News page instead of four separate news feeds, one Economic Data page instead of three chart grids, one Calendar instead of three event lists. Scrapers/generators are still one-per-source internally (see Data flow above); the merging into unified pages happens once, in `core/generate_dashboard.py`, not in the underlying pipeline.

- **Cockpit** — Market Overview only (Equities/Interest Rates/Exchange Rates/Commodities cards). No tabs — this is the one page that stayed single-purpose, since "market data" doesn't fit the news/data/calendar shape the other three sources share. Commodities: Brent Crude, Iron Ore, Natural Gas, Diesel, Gasoline, Gold, Silver, Copper, Aluminum, Nickel, Jet Fuel (11 total — the old Suzano/Klabin pulp-price proxies were removed, see §4). Every card on every group — not just Commodities — carries a small "ⓘ" icon (hover tooltip, `ICON_INFO`) showing exactly which data provider it came from (`item.source` in `core/generate_dashboard.py`'s `_ck_make()`).
- **News** — unified feed: Portfolio + Brazil + China + Credit + GDELT articles, one list sorted strictly by recency (never resorted by importance). Filters: search box, **Country** (Brazil/China — Portfolio and Credit are both tagged Brazil since portfolio companies are B3-listed and credit news is the Brazilian credit market), **Sector** (one flat merged list across all sources — Credit's categories like "Recuperação Judicial" sit in the same dropdown as Brazil's/China's sector names), **Company** (populated only from the ~10 portfolio-tracked companies — the only source with company-level data), **Source** (multi-select checkbox list, e.g. select CNN Brasil + Valor Econômico simultaneously — `news_sources` is every distinct `a.source` value across the whole unified feed, ~174 with China's feed included since that one isn't domain-whitelisted; the checkbox list has its own client-side search-within-filter to stay usable at that count, separate from the main headline search box). **News Clipping**: "Generate Clipping" button in the feed header → checkboxes appear on every card → "Export Excel" downloads a real `.xlsx` (Date/Title/Link, Link as a genuine clickable hyperlink) of just the selected articles — see §4 for the implementation.
- **Economic Data** — unified chart grid: all Brazil (BCB) + China (chinadata.live) + Credit (BCB) dataset cards in one flat grid, client-side-tagged by source and filterable via pills (All/Brazil/China/Credit). The merge happens in the browser (`buildEconCharts()` in the template's JS combines the three `*_DATASETS` JS globals with a source tag) — no Python-side data model change was needed for this page.
- **Calendar** — unified event list: Brazil + China events, with Portfolio's (Cockpit's) earnings/COPOM events folded into the Brazil bucket. Filter: Country pills (All/Brazil/China).
- **Other Datasets** — the China dataset catalog browser (`buildCatalog()`), relocated to its own top-level page. Contents unchanged, stays China-only.

### Shared chart UI (standardized across every chart on every page)
- Every chart renders with Plotly's native modebar **off** (`displayModeBar:false`). Each card instead has small icon buttons in its header: **PNG**, **CSV**, **Expand**.
- **Expand** opens one shared modal (`#chartModal` / `.chart-modal*` CSS, `chartExpand()` in the template's JS) used by every chart — not a per-page component.
- Cockpit's FX cards (Exchange Rates group) are stat-only by design — big number + CSV download, no chart, no expand.
- The Economic Data page's chart grid is **one flat grid** for all 3 sources combined (no per-sector or per-source section breaks — see §4 for why); sector is shown via a colored left border + small tag per card, source via the pill filter, instead of hard row/page breaks.

---

## 3. Coding Standards

- **No comments explaining *what*** — code is expected to be self-descriptive via naming. Comments are reserved for non-obvious *why* (a workaround, a confirmed-bad external API, a subtle invariant).
- **Every scraper/event-generator file is self-contained**: each defines its own `HEADERS`, does its own `urllib3.disable_warnings()` + `sys.stdout.reconfigure()` at import time, computes `ROOT = Path(__file__).resolve().parent.parent`. This duplication is intentional — matches the project's "few files, each independently runnable" philosophy — except for genuinely cross-cutting concerns (HTTP retry logic in `core/net_utils.py`, importance-score bucketing in `core/scoring.py`, and the trusted-domain whitelist in `core/trusted_sources.py` — the last one specifically because 3 scrapers must apply the exact same rule, where duplication would risk drift).
- **All HTTP calls use `verify=False`** + `urllib3.disable_warnings(InsecureRequestWarning)` — required because the target corporate network (Itaú) intercepts HTTPS via a proxy with its own CA not in the `certifi` bundle. This is deliberate, not an oversight — do not "fix" it by re-adding cert verification without checking with Carlo first.
- **All HTTP calls go through `core.net_utils.get_with_retry()`** (2 attempts, backoff) rather than raw `requests.get()` — single-attempt calls were the root cause of a whole class of "0 articles fetched" bugs on flaky/corporate networks.
- **Windows compatibility:** every entry-point script calls `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` and every `subprocess.run()` passes `encoding="utf-8"` — Windows terminals default to CP1252 and choke on unicode (`→`, `—`, `…`) otherwise.
- **Timezone discipline:** all article/display timestamps must be in **Brasília time** (`America/Sao_Paulo`), not raw UTC from feed sources. Centralized in `parse_date()` in `core/generate_dashboard.py` — convert there, not at each call site.
- **No premature abstraction:** the 4 scrapers and 3 event generators are near-duplicates of each other by design (different queries/sectors) rather than parameterized into one generic module — this was a deliberate choice matching "few files, easy to reason about one at a time" over DRY-at-all-costs.
- **Testing:** none currently. No test suite, no CI. Verification during this project's development has been manual: run the actual scraper/generator, inspect real output counts, syntax-check generated JS with `node --check`.

---

## 4. Important Decisions

Chronological, most-recent last. Only decisions with lasting architectural consequence — see git log for the full incremental history.

- **Windows CP1252 fix** — `sys.stdout.reconfigure()` + `encoding="utf-8"` everywhere. *Why:* Windows terminal crashed on unicode output. *Impact:* every script touches this pattern at import time.
- **`verify=False` on all requests** — bypasses the corporate SSL proxy's untrusted CA. *Why:* certifi-based fixes didn't work; this is the bank's network doing TLS interception, confirmed by Carlo. *Impact:* `certifi` dependency removed entirely.
- **Fully static output, no server** — removed the refresh button and (later) deleted `core/server.py` entirely. *Why:* the real usage pattern is generate-then-email/upload, not "browse a running server." *Impact:* `dashboard.html` has zero `fetch()` calls; all chart/news data is baked in as inline JSON at generation time.
- **News window: 1 day → 7 days, fixed in two places** — raising `MAX_NEWS_AGE_DAYS` in `generate_dashboard.py` alone wasn't enough; each scraper had its *own* `MAX_AGE_DAYS=1` and a `balance_by_day()` that only bucketed "today" vs "yesterday", silently discarding anything older even after the render-time gate was widened. *Fix:* raised `MAX_AGE_DAYS` to 7 in all 4 scrapers + added a third "older" fallback bucket to `balance_by_day()` in 5 places (4 scrapers + `generate_dashboard.py`). *Lesson:* when a filter exists in multiple layers, check all of them.
- **Retry logic (`core/net_utils.get_with_retry`)** — single-attempt `requests.get()` calls were silently zeroing out entire sectors/datasets on transient timeouts. *Impact:* wired into every fetch site; bumped `update_dashboard.py`'s per-script subprocess timeout from 180s → 600s to give retries headroom without false-timeout aborts.
- **PIX BHKP/NBSK pulp proxies → Suzano/Klabin stocks** — FOEX's real pulp index is paywalled; the FRED series originally used (WPU0912, US PPI) was a meaningless proxy. *Fix:* replaced with `SUZB3.SA` / `KLBN11.SA` (B3-listed, liquid, fetchable via yfinance) as pragmatic proxies. **Superseded**: Carlo asked to remove these entirely (see the Cockpit commodities-expansion entry below) — no replacement pulp/paper metric exists on the Cockpit anymore.
- **Brazil 10Y: 12511 (broken) → Selic (wrong metric) → ANBIMA ETTJ (correct, real)** — first swapped the broken BCB series 12511 for the Selic policy rate as an honest-but-different fallback; Carlo rejected that outright ("the values of the brazilian interest is not actually the future curve but actually the selic, which is not the same thing"). Found ANBIMA's free daily ETTJ download (`https://www.anbima.com.br/informacoes/est-termo/CZ-down.asp?Dt_Ref=DD/MM/YYYY`) — a real, market-derived nominal yield curve, verified against independent reporting (fetched 14.44% vs. investing.com's reported 14.43% for the same day). The free endpoint serves one date per request and only retains a few months of history, so `fetch_br10y()` accumulates a local `data/br_ettj_history.json` (same merge/append pattern as the news caches): a short backfill on first run, then just the newest day appended each subsequent run.
- **File reorg: root-level `update_dashboard.py` + `setup.py`, `core/server.py` deleted** — matches actual usage: Carlo runs `update_dashboard.py` twice daily, `setup.py` once per machine, teammates only ever receive `dashboard.html`. Also deleted `templates/china_template.html` (610 lines, unreferenced dead file from before China was folded into the unified template).
- **Cockpit Market Overview: above news → below news → own "Market Data" tab (first, default-active)** — iterated per direct user feedback; settled on tab pattern to match Brazil/China/Credit's existing tab-first convention.
- **Chart UI standardization** — disabled Plotly's native modebar everywhere (was overlapping tiny 100px Cockpit cards); unified every chart card to the same PNG/CSV/Expand icon-button row; generalized the Cockpit-only "mkt-modal" into one shared `.chart-modal` used by all 4 pages. Also fixed a bug where the modal plotted its chart *before* being shown (`display:none` at plot time → zero-width render) — fixed by show-then-wait-two-frames-then-plot.
- **Removed per-sector section headers in Economic Data grids** — each sector previously started its own CSS auto-fill grid row, leaving visible blank slots whenever a sector's dataset count didn't evenly fill a row. Flattened to one grid per page; sector now shown via a colored left border + small tag instead of a hard row break.
- **Itaú logo embedded as base64** — resized the original 3840×3840 source (`Itaú.jpeg`) down to 120×120 (`assets/itau_logo.png`) before embedding, to avoid bloating `dashboard.html` by ~500KB for a header icon.
- **Timestamps converted to Brasília time at the source** — Google News RSS publishes in UTC; `parse_date()` (the single choke point used by all 4 news feeds) now converts via `zoneinfo.ZoneInfo("America/Sao_Paulo")` immediately after parsing, so every downstream display (article time, header "Updated" stamp) is correct without touching each call site individually.
- **News importance badges (Low/Medium/High)** — every scraper already computed a `relevance_score()` to decide what clears `MIN_SCORE`, then discarded it (`del a["_score"]`). Kept it instead: `core/scoring.py` buckets it (`importance_bucket()`, thresholds High ≥8 / Medium ≥5 / Low below, same on every page), each scraper persists it as `importance_score` + `importance`, and the template shows it as a small badge on every news card. Sort order stays strictly recency-based per explicit instruction — importance is informational only, not a resort key. Thresholds were calibrated against real re-scraped data, not guessed: every bucket has meaningful representation on all 4 pages (see git log commit `1e731d9`).
- **Brazil scraper: 7-day window → strict same-day, generic keywords → technical per-sector keywords** — Carlo wanted Brazil news restricted to today only (Brasília calendar date) with analyst-level search terms per sector instead of generic ones, while still hitting a 30-article floor per sector. Discovered via direct testing that Google News RSS ranks results by *relevance*, not recency, and caps each query at ~100 entries — a single broad OR-query per sector returned **zero** today-dated articles out of 100 results, because the query's relevance budget got spent on one dominant sub-topic. Fixed by querying each keyword *separately* (one request per term, not one OR-mega-query), which surfaced real same-day results the mega-query missed entirely. Confirmed empirically that some niche sectors (Pulp & Paper, Energy) genuinely don't have 30 same-day articles most of the time — the 30-floor is best-effort with score-based backfill (same-day candidates only, never reaching into other days), not a guarantee, and that's a real content-volume ceiling, not a bug.
- **Major reorg: 4 source-pages → 5 content-type pages** — Cockpit/Brazil/China/Credit (each reimplementing its own News/Events/Charts tabs) replaced with Cockpit/News/Economic Data/Calendar/Other Datasets, each internally filterable instead of hard-partitioned by source. Planned via `EnterPlanMode` given the scope (touches nearly all of `templates/dashboard_template.html` and `core/generate_dashboard.py`'s render pipeline); full plan and the resolved ambiguities (country tagging, no separate "topic" axis, Calendar scope) are in the approved plan — see git log commit for this reorg for the plan file reference. Key resolutions: Sector is one flat merged list (Credit's categories sit alongside Brazil's/China's, no separate "topic" filter); Portfolio and Credit news/events are tagged `country="Brazil"` (Portfolio companies are all B3-listed, Credit news is the Brazilian credit market); Portfolio's COPOM entries are deduplicated against Brazil's (both generators independently schedule COPOM with byte-identical titles) rather than shown twice. Scrapers/event-generators themselves are untouched — the merge happens once, in two new functions in `core/generate_dashboard.py` (`load_unified_news()`, `load_unified_events()`), keeping each source independently refreshable.
- **Source whitelist: trust as a scoring bonus → trust as a hard domain filter** — Carlo flagged that "many articles are from unreliable sources" and supplied a ~130-outlet whitelist (Brazilian financial/business/trade press, wires, government agencies, sector trade publications). Previously each of the 3 Brazil-market scrapers (`scraper.py`, `brazil_scraper.py`, `credit_scraper.py`) had its own `TRUSTED_SOURCES` set of outlet *names*, used only as a `relevance_score()` bonus (+3/+2) — an untrusted-source article could still clear `MIN_SCORE` and get kept. Replaced with a hard pre-filter: `core/trusted_sources.py` (new shared module, `TRUSTED_DOMAINS` + `is_trusted(href)`) is checked against `entry.source.href` — Google News RSS's real publisher-domain field, confirmed via direct testing (`entry.source == {"href": "https://valor.globo.com", "title": "Valor Econômico"}`) to be more reliable than matching on `entry.source.title` (matching `entry.link`, a `news.google.com` redirect URL, isn't possible directly). Any article whose domain isn't in the whitelist is discarded before scoring; the now-redundant trust bonus was removed from all 3 `relevance_score()` functions. **Kept as one shared module** (unlike each scraper's usual self-contained-duplication convention, see §3) specifically because all 3 must apply the identical rule — one list avoids drift if Carlo asks to add/remove a domain later. Deliberately **not** applied to `china_scraper.py` — the whitelist is 100% Brazilian outlets; applying it there would zero out the China feed entirely. The existing caches held articles fetched under the old (bonus-only) rule, and `merge_articles`'s freshness-based carryover doesn't retroactively re-check trust on cached rows — so the change wouldn't have taken full effect until those aged out (up to 7 days for Portfolio/Credit, same-day for Brazil). Purged all 3 caches (`data/news_cache.json`, `data/brazil_news_cache.json`, `data/credit_news_cache.json` — gitignored, regenerable, not source data) and re-ran fresh rather than waiting. Confirmed post-purge outlet lists are 100% whitelist domains. Verified via live run: `brazil_scraper.py`'s 30-per-sector floor is no longer met at all post-purge (Economy 5, Energy 2, Mining & Steel 2, Pulp & Paper 0, Agriculture 9, Financial 14, Industry 10, Trade & FX 49 — 91 total) — this is the same "real content-volume ceiling, not a bug" situation as the earlier same-day-only change, now compounded by the domain restriction; flagged to Carlo rather than silently loosening `MIN_SCORE` or the date gate to compensate, which would defeat the point of the whitelist. Portfolio dropped to 70 total across 10 companies (Bradesco: 0). Credit dropped to 29 total across 7 sectors.
- **Added GDELT as a second, independent portfolio-news source (`gdelt_scraper.py`)** — Carlo asked why Bradesco (one of Brazil's largest banks) returned 0 whitelist-passing articles and whether a better scraping method existed. Root-caused with live queries, not assumption (same standard as the Brazil same-day fix): `scraper.py`'s Bradesco query returns Google's full 100-result cap, but only 7 of those 100 fall inside the 7-day freshness window, and only 1 of those 7 was from a whitelisted domain — and that 1 scored below `MIN_SCORE` because its headline never said "Bradesco" (Google's phrase match is against the full article body, not the title). This is the *exact same* OR-mega-query relevance-dilution problem already diagnosed and fixed for `brazil_scraper.py` — `scraper.py` was never given the same per-keyword treatment, and a single-keyword test (`Bradesco BBDC4`) confirmed a full page of good, recent, trusted-domain results existed all along. The durable fix for that (per-keyword queries in `scraper.py`/`credit_scraper.py`) is still outstanding — GDELT was pursued as the *complementary* ask actually made. Live-tested GDELT's DOC 2.0 API (`api.gdeltproject.org/api/v2/doc/doc`) directly before writing any code: confirmed working query syntax (`sourcelang:portuguese`, `sourcecountry:brazil`, `sourcelang:english`), confirmed it returns the real publisher domain directly (`domain` field — no Google-redirect problem to work around), and confirmed its rate limit is real and unforgiving: a burst of unspaced test requests triggered a 429 lockout that was still active 30+ minutes later (confirmed via repeated, deliberately spaced re-checks — not a single data point), despite GDELT's own error message only stating "one request every 5 seconds." Built `gdelt_scraper.py` accordingly: throttles to 1 request/6s minimum, treats 429 as a hard stop (one retry after a 60s backoff, then abort and keep whatever was already fetched) rather than retrying aggressively into a worsening lockout. Scoped to **Portfolio companies only** (not Brazil/Credit sectors) for this first pass — same query taxonomy as `scraper.py` (reads `portfolio.xlsx` directly), two query legs per company (Portuguese restricted to `sourcecountry:brazil`, English unrestricted since international wires covering Brazilian companies aren't Brazil-sourced), each leg hard-filtered to a domain whitelist exactly like the Google News scrapers — GDELT itself does zero relevance ranking or quality filtering, so the whitelist matters *more* here, not less. Added `TRUSTED_DOMAINS_EN` to `core/trusted_sources.py` (Reuters, Bloomberg, FT, WSJ, CNBC, MarketWatch, Barron's, Forbes, Business Insider, S&P Global, Moody's, Fitch Ratings, AP, The Economist, BBC) since the existing `TRUSTED_DOMAINS` is Brazil-only and the English leg needed its own curated list — this restores the "international wires" tier that used to live locally in `scraper.py`/`credit_scraper.py` as a scoring bonus before the source-whitelist change above replaced it with a hard Brazil-only gate. Also added `DOMAIN_TO_OUTLET` (a reverse domain→outlet-name lookup, built from the same table `TRUSTED_DOMAINS` was normalized from) and `outlet_name()`, since GDELT's `domain` field is a bare hostname with no outlet title attached (unlike Google News RSS's `entry.source.title`). Output (`data/gdelt_news_cache.json`) is written in the exact same company-keyed shape as `news_cache.json` so it flows through the existing `flatten_news()` loader unchanged; merged into `load_unified_news()` as a 5th origin (`origin="gdelt"`, `country="Brazil"`), inheriting the same JS-assigned sector/company badge coloring as Portfolio rows (both lack a `color` field on the article dict, which is what the template actually branches on, not the origin tag). Wired into `update_dashboard.py`'s `STEPS` list right after the existing "Portfolio news" step. No new dependency — `requests` and `pandas` were already in `requirements.txt`.
- **`gdelt_scraper.py` redesigned for a ~150-company portfolio before it ever ran once at 10** — Carlo mentioned the final portfolio list will have 150 companies, not 10. Did the math before touching code: at 1 request/company/leg (the original design), 150 companies × 2 languages × the 6s throttle floor = **~30 minutes**, which blows `update_dashboard.py`'s 600s per-step timeout — and since the scraper only wrote its cache once at the very end of `main()`, a timeout kill would have discarded the *entire* run's progress, not just the excess. Asked Carlo two things before redesigning: whether the 150-company list would include tickers (**no — names only**) and how to handle the runtime problem (**"try batching companies per GDELT query first"**). Redesigned around both answers:
  - **Batched queries**: companies are grouped `BATCH_SIZE` (currently 5) at a time into one OR'd query (`("Petrobras" OR "Vale" OR ...) sourcelang:...`) instead of one request per company. This is **not empirically verified against GDELT's real query-complexity ceiling** — development testing tripped GDELT's rate limit before this could be confirmed live (see the rate-limit constraint below), so `BATCH_SIZE=5` is a conservative starting guess, not a measured safe value. Flagged prominently in the module docstring and as a `CLAUDE.md` follow-up (§7) — the first real run needs to be watched for silently thin/skewed per-company results, which would indicate the batch is too complex for GDELT to handle cleanly.
  - **No-ticker disambiguation**: added an optional `Ticker` column to `portfolio.xlsx` (blank is fine — falls back to the bare company name) instead of a hardcoded Python dict (`AMBIGUOUS_TERM`), which doesn't scale to 150 rows without silently degrading for any company someone forgets to add. Populated it for the current 10 companies as a working example. Article-to-company attribution (needed now that a batch's response is a shared pool across several companies) checks **both** the GDELT query term (which may be a ticker) **and** the plain company name as whole-word matches in the title — checking only the query term was tried first and caught by a quick offline unit test (a synthetic "Vale...Bradesco" ambiguous-batch case revealed articles using the company name rather than the ticker in the headline would go completely unattributed, silently undercutting GDELT's whole point). Zero or multiple companies matching in one article is dropped, not guessed at.
  - **Incremental checkpoint writes**: `gdelt_news_cache.json` is now written after every batch, not once at the end — a timeout kill (or a 429 lockout mid-run) now loses at most the in-flight batch, not the whole run. `cache` starts as a copy of the existing on-disk cache, so companies not yet reached in a partial run keep their last-known-good data rather than disappearing.
  - Net effect at `BATCH_SIZE=5`: 150 companies → 30 batches × 2 languages = 60 requests × 6s ≈ 6 minutes, comfortably inside the timeout (vs. the original design's ~30 minutes). If live testing shows `BATCH_SIZE` needs to shrink (complexity-limit errors, or results skewed toward whichever company sits first in the OR clause), the runtime math should be redone — even `BATCH_SIZE=3` (50 batches × 2 × 6s ≈ 10 min) stays inside budget; anything below that would need a rethink (e.g. splitting the 150 companies across multiple runs on a rotation, an option Carlo didn't pick but that's still on the table if batching alone doesn't hold up).
- **News page: added a Source filter, multi-select** — Carlo wanted to filter headlines by outlet (e.g. CNN Brasil + Valor Econômico at once), which the existing Country/Sector/Company filters (all single-select `<select>` dropdowns) couldn't do. `load_unified_news()` now also returns `news_sources` (every distinct `a.source` across the whole unified feed — **174** at the time this was built, since China's feed isn't domain-whitelisted the way Brazil/Credit/Portfolio/GDELT are, so it pulls in a long tail of international outlets). At that count a plain checkbox list needed its own small search-within-filter to stay usable — a `#newsSourceSearch` input that just shows/hides `.src-item` labels client-side, entirely separate from the main headline search box (`#newsSearch`, which still searches `data-q`). Implemented as checkboxes (`.src-checkbox`), not a native `<select multiple>` — clicking a checkbox is a more discoverable "select several" affordance than ctrl/cmd-click on a listbox, and matches how a filter-sidebar checkbox list conventionally reads. The News filter JS's `apply()` gained one more AND-condition (`sources.size === 0 || sources.has(card.dataset.source)`) — empty selection means "no filter," consistent with every other filter dimension on this page defaulting to "All."
- **Cockpit: removed Suzano/Klabin pulp proxies, added 9 more commodities, added per-card source attribution to every metric on the page** — Carlo asked to drop the pulp & paper proxy cards (see the superseded entry above) and add Natural Gas, Diesel, Jet Fuel, Gasoline, Gold, Silver, Copper, Aluminum, Nickel, plus a visible source for every commodity *and* every other Cockpit metric (Equities/Interest Rates/Exchange Rates too). Verified every new ticker live before wiring anything in, the same standard as the Brazil-10Y/ANBIMA work — this surfaced two real data-availability problems that were resolved honestly rather than papered over:
  - **Jet Fuel has no clean free outright price.** Yahoo Finance's `JET=F` looked plausible until its full 6-month series was pulled: it oscillates between -0.48 and +0.50 with a mean near zero — a differential/crack-spread contract, not an outright price (an outright jet fuel price going *negative* would be a huge red flag on its own). Initially shipped as an honest "N/A" card — but Carlo flagged it (along with Nickel) as a "broken chart" in the next review pass, which is a fair read: a permanently-empty card looks like a bug even with a clear label. **Superseded**: removed from the Cockpit entirely rather than kept as a standing N/A — see the follow-up fixes entry below.
  - **Nickel has no Yahoo Finance ticker at all** (`LNI=F`, `NI=F`, `NID=F`, `NICKEL` all confirmed 404/delisted live). FRED's `PNICKUSDM` ("Global price of Nickel") is the only free option found — used as the sole source, labeled `"FRED (monthly)"` since it's materially lower-frequency than every other (daily) card on the page; that distinction is surfaced in the tooltip, not hidden.
  - **Aluminum**, by contrast, turned out to have a real, verifiable Yahoo Finance ticker (`ALI=F`) — confirmed via `yf.Ticker().info` returning `"Aluminum Futures, Sep-2026"` on COMEX, not a coincidentally-valid-but-wrong symbol — with FRED's `PALUMUSDM` wired as a fallback, same two-tier pattern as Iron Ore.
  - **Diesel and Gasoline use the standard commodity-market benchmark contracts** for those fuels (`HO=F` NY Harbor ULSD, `RB=F` RBOB) rather than a literal "diesel"/"gasoline" ticker, which don't exist as such — labeled honestly (`"Diesel (NY Harbor ULSD)"`, `"Gasoline (RBOB)"`) so nobody mistakes the benchmark grade for the label, the same spirit as the Suzano/Klabin proxies this replaces, just for a genuinely fungible fuel grade instead of an unrelated equity.
  - **Source attribution UI** (v1, superseded — see follow-up entry below): every cockpit card showed a small "ⓘ" icon with a `title="Source: ..."` hover tooltip. Carlo reported this "not displaying correctly" — replaced with an always-visible text label. For metrics with a primary/fallback fetch path (Iron Ore, Aluminum), the source shown is **whichever path actually returned data that run**, determined at fetch time — never a static guess, consistent with the project's standing "never mislabel a data source" rule; this part of the design held up and carried over.
  - **Grid layout** (v1, superseded — see follow-up entry below): the Commodities group grew from 4 to 11 cards, which would have broken the old `grid-template-columns:repeat({{ keys|length }},1fr)`. Changed just that group to `repeat(auto-fill, minmax(150px, 1fr))` — Carlo reported this still felt cramped; widened further, see below.
- **6-bug fix round on the Cockpit/News/Economic Data pages, filed together after Carlo actually reviewed the dashboard** — each one root-caused individually rather than patched on symptom:
  - **Source tooltip "not displaying correctly"**: the info-icon's SVG dot used `r="0.6"` in a 24-unit viewBox — compare the already-working `ICON_PNG` icon's dot, `r="1.4"`, more than 2x larger. At 12px render size, `r="0.6"` is close to invisible, and a hover-only tooltip is also just a bad fit for a document meant to be reviewed statically (email/SharePoint), not necessarily hovered over. Fix: dropped the icon and hover tooltip entirely; every cockpit card now shows its source as a small always-visible `.mkt-card-source` text line (9px, muted, non-bold — placed so it doesn't clash with the existing all-caps `.mkt-card-name` label style it sits below).
  - **Jet Fuel and Nickel "broken"**: Jet Fuel — no real fix exists (see above), so it's removed from `COCKPIT_GROUPS` entirely rather than kept as a standing N/A card, which reads as broken regardless of how clearly it's labeled. Nickel — this one *did* have a real, fixable bug, not just bad luck: `_ck_fred()` was hanging to timeout on every call inside the full pipeline (confirmed: two `get_with_retry` attempts, ~58s total, both timing out) while an identical direct request succeeded in under 1s moments later. Isolated the variable by testing with and without `core.net_utils.DEFAULT_HEADERS`' browser-spoofed User-Agent (the one that exists specifically to get past Google News' bot detection) — confirmed *deterministically*, not just correlated, that FRED's `fredgraph.csv` endpoint hangs on that Chrome UA and responds instantly to a plain one. `_ck_fred()` now passes its own explicit non-browser UA instead of inheriting `get_with_retry`'s default. This was misdiagnosed as "occasional cold-start latency" one review cycle earlier (a timeout bump from 15s→25s was tried first and didn't fix it, because the real cause was never about timing) — the lesson: when a timeout bump doesn't fix a timeout, the timeout wasn't the actual problem, go looking for what's actually different about the failing request.
  - **Commodities grid "cramped"**: `minmax(150px, 1fr)` → `minmax(200px, 1fr)`, `.mkt-grid` gap 8px→10px (12px→14px for the Commodities group specifically, new `.mkt-grid-wrap` modifier class), `.mkt-group` margin-bottom 14px→20px, chart height 100px→110px. Also incidentally freed up header space by removing the 3rd (info) icon button from the action row — 2 icons (CSV/Expand) in a ~200px-wide card reads much less cluttered than 3 did in a 150px one.
  - **FX pairs "inconsistent"**: audited all 4 by fetching each ticker live and checking against known real-world rates. Found one real bug: `USDBRL=X` (which yfinance quotes as BRL-per-1-USD, ~5.10) was keyed `brl_usd` and labeled `"BRL / USD"` — backwards. By the same base/quote convention the *other* 3 pairs already followed correctly (`usd_cny`→"USD / CNY" = CNY-per-USD; `eur_brl`→"EUR / BRL" = BRL-per-EUR; the computed `brl_cny`→"BRL / CNY" = CNY-per-BRL), this should have been `usd_brl`/"USD / BRL". Renamed the key and fixed the label; the computed BRL/CNY fallback (which reads `data["usd_brl"]["data"]`) was updated to match and re-verified — its actual arithmetic was already correct, only the now-consistent key name needed updating.
  - **China dataset "no longer working, started after the refactor"**: root-caused by writing a script that cross-references every `getElementById()` call in the template's JS against every `id="..."` actually defined in the HTML — found exactly one dangling reference: `buildCatalog()` (the Other Datasets page) calls `document.getElementById('cnCatalogBadge').textContent = ...`, but `cnCatalogBadge` was the old per-source-tab dataset-count badge, deleted along with the rest of the tab-nav UI during the 5-page reorg and never updated at this one call site. `getElementById` on a missing id returns `null`, and `null.textContent = ...` throws immediately — which halts `buildCatalog()` before it builds anything, so the entire Other Datasets page (category filter + all 337 catalog cards) silently rendered empty. This was never a chinadata.live API problem — `fetch_china_charts()`/`fetch_china_catalog()` were both re-verified live and returned all 15 chart datasets and all 337 catalog entries correctly the whole time; the bug was 100% front-end, introduced by the reorg deleting an element an unrelated function still referenced. Fix: removed the dead line (the dataset count is already shown via `#catCount`, this badge was redundant even before it broke).
  - **Footer attribution wrong on every page except Other Datasets**: `"Portfolio Intelligence · Charts via chinadata.live"` was a single static string outside all `.page` divs, so it showed on Cockpit/News/Calendar too, none of which touch chinadata.live at all. Made it dynamic: `FOOTER_ATTRIBUTION` (JS object, one line per page, matching what that page actually fetches — Cockpit: Yahoo Finance/FRED/ANBIMA; News: Google News/GDELT; Economic Data: BCB/chinadata.live; Calendar: BCB/IBGE/Yahoo Finance — Calendar's China events are pattern-computed with no live API call at all, so chinadata.live is deliberately *not* listed there; Other Datasets: chinadata.live, the one page where the original claim was already correct), swapped in by `showPage()` alongside the existing tab-switch logic.
- **News Clipping** — interns were manually copying articles into an Excel sheet by hand; Carlo wanted a "Generate Clipping" button, multi-select, and a real Excel export (Date/Title/hyperlinked Link) with as few clicks as possible. Landed on: click "Generate Clipping" once (reveals a checkbox on every news card, replaces the button with a live "N selected" counter + Export + Cancel) → click each article once → click "Export Excel" once → `.xlsx` downloads immediately, no confirmation dialog or review screen in between. That's 2 + N clicks total for N articles, which is close to the minimum achievable for a genuine multi-select-then-export flow.
  - **Real `.xlsx`, not a CSV-with-formula trick**: a `=HYPERLINK(...)` formula string in a `.csv` *can* render as a clickable link when Excel auto-imports it, but that behavior is inconsistent across Excel versions/platforms and is the same mechanism as the "CSV injection" security pattern — modern Excel sometimes warns on or sanitizes formula-looking CSV content. Added SheetJS (`cdn.sheetjs.com/xlsx-0.20.2`, pinned version) as a 2nd CDN `<script>` tag alongside Plotly's, generating a genuine `.xlsx` with a real embedded hyperlink relationship (`ws[cell].l = {Target: url}`) — reliable regardless of Excel version, and this is a frontend-only addition, no new `requirements.txt` entry (same category as Plotly: a browser dependency, not a Python one).
  - **Verified as a real file, not just JS syntax**: downloaded the actual pinned SheetJS bundle and ran the exact export logic (data-building, sorting, hyperlink-cell-attachment) in Node against synthetic article data, wrote a real `.xlsx`, then *read it back* and confirmed the header row, sort order (newest first), Portuguese DD/MM/YYYY dates, and — critically — that the Link column's cells carry a genuine `External` hyperlink relationship pointing at the right URL, not just visible text that looks like one. Also verified the fallback path: an article with an unparseable date correctly produces a blank Date cell (sorts to the end) rather than crashing or showing garbage — same "no data" honesty standard as the rest of this project, just applied to a client-side export instead of a server-side fetch.
  - **Selection survives filtering and page-switching for free**: checkbox `.checked` state and a `clip-selected` CSS class live directly on each `.news-card` DOM element, which is never removed or re-rendered — filters only toggle `display:none`, and switching pages only toggles a `.page.active` class — so no extra state-tracking code was needed to keep selections intact if a user filters the list or checks another tab mid-selection.
  - **Event handling**: the existing "click card to open article" listener is bound directly on each card (bubble phase). The new clip-mode intercept is registered on `#newsFeed` with `{capture:true}`, so it always runs first regardless of code order, and calls `stopPropagation()` to prevent the old listener from ever firing while in clip mode — clicking a card selects it instead of opening a new tab. Clicking the checkbox itself is special-cased to *not* call `preventDefault()` (which would cancel the browser's own checkbox toggle), only `stopPropagation()`.
- **PROTOTYPE, not committed — Chart → PowerPoint export, integrated from a teammate's project** — Carlo has a separate project, `distribuicao-combustiveis-completo/` (now a subfolder of this repo, its own nested git repo — a React/Vite/Supabase scaffold generated by Lovable, but the actual working part is `public/dashboard.html`, a static file the React shell just redirects to, same "generate a static file" spirit as this project). Asked me to study how it generates charts/PPTX slides and add a "chart → PowerPoint slide" export option to this dashboard's charts (Cockpit especially), matching *this* project's UI but *that* project's slide styling. Explicitly asked for a prototype only — **not committed, reviewable/revertable**.
  - **What their project actually does**: Chart.js for on-page charts, **PptxGenJS** (`pptxgenjs@3.12.0`, CDN-loaded, same "browser-side library, no server" pattern as Plotly/SheetJS in this project) for PPTX generation — a `.btn-ppt` button calls `exportarPPT()`, which builds one 16:9 slide with 4 charts (2 bar + 2 donut) using PptxGenJS's **native chart objects** (`pres.charts.BAR`/`DOUGHNUT`), not pasted screenshots — meaning the exported charts stay editable in PowerPoint. Slide design: light-gray (`F5F5F5`) background, thin gray divider line under each section title, Calibri throughout, small muted "Fonte: ..." source line top-right, custom-built compact legends (colored ovals + text) instead of PptxGenJS's default legend.
  - **What was integrated vs. adapted**: reused the library choice (PptxGenJS, same pinned version), the slide design language (light-gray background, thin divider under title, Calibri, top-right source attribution, native editable charts instead of images), and the general "click icon → build presentation → `pres.writeFile()` triggers download" flow. Deliberately **did not** reuse their exact composition (one slide, 4 charts, bar+donut) — this dashboard's ask was per-chart export off the existing PNG/CSV/Expand icon-button row, so each export is **one chart → one slide**, using `pres.charts.LINE` (their project's data was categorical volume/market-share; this dashboard's is almost entirely daily time series). Also deliberately **did not** reuse their orange brand palette (`FF6201`) — each exported chart uses that same metric's own color already assigned in `COCKPIT_DATA`/dataset definitions, so the on-screen chart and the exported slide always agree; forcing every chart into someone else's unrelated brand orange would have undercut "the exported slide should look like it belongs to this dashboard."
  - **UI**: a 4th icon button (`ICON_PPTX`, matching the existing PNG/CSV/Expand SVG-icon family exactly — 12px stroke-based SVGs, same `.chart-icon-btn` class) added to *every* chart card with a real chart — Cockpit's `.mkt-card-actions` (14 chart-type cards; the 4 FX stat-only cards don't get it, same as they don't get PNG/Expand today) and Economic Data's `.chart-actions` (all ~30 Brazil/China/Credit dataset cards, via the existing `buildEconCharts()` template-literal builder). One shared function, `exportChartToPPTX(btn, ref)`, handles both — `ref = {source:'cockpit', key}` reads `COCKPIT_DATA`; `ref = {source:'econ', id}` reads a new `window._econCombined` global (the `combined` array `buildEconCharts()` already builds internally, just not previously exposed outside that function) and handles both China's multi-series shape and Brazil/Credit's single-trace shape, mirroring the exact same two-shape branching `buildEconCharts()` itself already uses for on-page rendering.
  - **Verified as a real file, not just JS syntax** — same standard as the Excel export: installed the npm-published `pptxgenjs` package (the CDN "full bundle" is browser-oriented, expects `window`/`Blob`, and can't `require()` directly in plain Node — same issue hit and solved the same way for SheetJS earlier), ran the actual export logic against realistic fake chart data, wrote a real `.pptx`, unzipped it, and confirmed by reading the raw slide/chart XML that the title, source line, footer text, and — critically — the chart's embedded data point values and date labels all made it into the file correctly, as a genuine native chart object (`ppt/charts/chart1.xml` with real `<c:v>` data values), not an image.
  - **Not yet reviewed in a browser, not committed** — per Carlo's explicit instruction this is a prototype for review. `git status` will show these changes as modified/untracked but nothing has been staged or committed. Before treating this as done: open the dashboard, click the new PPTX icon on a real chart, confirm the downloaded file opens correctly and looks right in actual PowerPoint (Node-level XML verification confirms correctness but not visual polish — font rendering, exact spacing, whether the axis labels overlap on a chart with many data points, etc.).

## 5. Dependencies

From `requirements.txt`:
- **pandas** — reads `portfolio.xlsx` (company/sector/keyword list driving the portfolio scraper and events generator).
- **openpyxl** — Excel engine for pandas.
- **feedparser** — parses Google News RSS responses.
- **requests** — all HTTP calls (wrapped by `core/net_utils.get_with_retry`, except `gdelt_scraper.py`'s GDELT calls, which need bespoke 429 handling — see §4).
- **jinja2** — renders `templates/dashboard_template.html` → `dashboard.html`.
- **yfinance** — equities/FX/rate/commodity price series for the Cockpit market overview and portfolio earnings dates.
- **lxml** — feedparser's XML parser backend.

Not in requirements.txt but used once, out-of-band: **Pillow** — used interactively to resize the source `Itaú.jpeg` down to `assets/itau_logo.png`. Not a runtime dependency of the build pipeline; don't add it to `requirements.txt` unless a future feature needs image processing at build time.

Removed: **certifi** (was part of an earlier, unsuccessful attempt to fix the corporate-proxy SSL issue; superseded by `verify=False`).

Frontend: **Plotly.js**, **SheetJS** (`xlsx-0.20.2`, News Clipping's `.xlsx` export), and **PptxGenJS** (`pptxgenjs@3.12.0`, prototype chart→PowerPoint export — see §4) via CDN `<script>` tags in the template (not an npm dependency — there is no `package.json`, no Node build step for the app).

---

## 6. Environment

- **No environment variables required** — no API keys. All data sources used are public/no-auth: Google News RSS, BCB OLINDA API, IBGE calendar API, chinadata.live, FRED public CSV endpoint, ANBIMA's free ETTJ download, Yahoo Finance (via yfinance).
- **External services relied on:** news.google.com (RSS), api.gdeltproject.org (DOC 2.0 API — free, no auth, but has a real and unforgiving rate limit, see §8), api.bcb.gov.br, servicodados.ibge.gov.br, chinadata.live, fred.stlouisfed.org, anbima.com.br, Yahoo Finance (via yfinance). All are free/public; none have documented SLAs — see §8 for the BCB series-12511 cautionary tale.
- **Commands:**
  - First-time setup (once per machine): `python3 setup.py`
  - Daily refresh (run twice a day): `python3 update_dashboard.py`
  - Individual pieces can also be run standalone for debugging, e.g. `python3 scrapers/brazil_scraper.py`, `python3 core/generate_dashboard.py` (uses whatever's already in `data/*.json`).
- **No build step, no test command, no deployment** — output is a single HTML file manually uploaded to SharePoint / sent to the team.

---

## 7. Current Progress

### Working
- All 4 Google News scrapers (portfolio, Brazil, China, Credit) — Brazil is strict same-day with technical per-sector keywords, the other 3 keep a 7-day window; all retry-hardened. The 3 Brazil-market scrapers (portfolio, Brazil, Credit) additionally hard-filter to a ~130-domain trusted-outlet whitelist (`core/trusted_sources.py`); China is unaffected.
- `gdelt_scraper.py` — a 2nd, independent portfolio-company news source (GDELT DOC 2.0 API, PT+EN), complementing `scraper.py`. Batches multiple companies per query (target: ~150-company portfolio, not the current 10) with dual-pattern (ticker + name) attribution and incremental checkpoint writes. Implemented, unit-tested offline, and code-reviewed, but **has never sent a real batched query** — see Known bugs below.
- All 3 event generators.
- Cockpit market overview (Equities, Interest Rates, Exchange Rates, Commodities) — 18 metrics, all with live data (Jet Fuel was removed — no free source exists; Nickel's FRED fetch was hanging on a browser-spoofed User-Agent, now fixed with an explicit non-browser UA, reliably live). Brazil rate metric is the real ANBIMA 10Y nominal yield curve (not Selic, not a proxy). Every metric shows its data source as a small always-visible text label (not a hover tooltip — that didn't display well, see §4). FX pairs (`usd_brl`/`usd_cny`/`brl_cny`/`eur_brl`) are all base/quote-consistent as of the §4 audit.
- **5-page reorg live**: Cockpit / News (unified 4-source feed) / Economic Data (unified 3-source chart grid) / Calendar (unified Brazil+China events) / Other Datasets (China catalog). Old Brazil/China/Credit source-pages no longer exist.
- Standardized chart UI (PNG/CSV/Expand) across every chart, shared modal.
- Brasília-time display for all article/header timestamps.
- Itaú logo in header.
- News importance badges (Low/Medium/High) on every card, sort still strict recency.
- News Clipping: multi-select articles on the News page, export a real `.xlsx` (Date/Title/hyperlinked Link) via SheetJS. Replaces interns' manual copy-into-Excel workflow.
- Root-level `update_dashboard.py` / `setup.py` workflow, verified end-to-end.
- `IDEAS.md` — prioritized backlog of future enhancements (Tier 1 item #1, importance scoring, shipped).

### Known bugs / technical debt
- None currently open that I'm aware of — verified the 5-page reorg structurally (page containers, JS syntax, unified row/event counts, COPOM dedup) but **has not been visually reviewed in an actual browser** — this environment can't render one. Recommend Carlo click through all 5 pages/filters before this goes to the team.
- **`brazil_scraper.py`'s 30-article-per-sector floor is now essentially never hit** since the domain whitelist (see §4) restricts the backfill pool before the floor logic runs — live-verified counts on a freshly-purged cache: Economy 5, Energy 2, Mining & Steel 2, Pulp & Paper 0, Agriculture 9, Financial 14, Industry 10, Trade & FX 49 (91 total, only Trade & FX comes close to 30). This is the intended tradeoff (fewer, more trustworthy articles) but worth flagging if Carlo asks why some sectors look thin — if he wants the floor honored, the whitelist would need to be widened, not the floor logic changed.
- **Portfolio scraper: Bradesco returned 0 trusted-domain articles** in the same post-purge run — root-caused (not just observed) as the same Google-relevance-ranking dilution problem `brazil_scraper.py` already had; `gdelt_scraper.py` was built specifically to address this, but see below for its own verification gap.
- **`gdelt_scraper.py` has never sent a real batched query, and `BATCH_SIZE=5` is an unverified guess** — development testing (confirming GDELT's basic query syntax, response schema) triggered its rate limit before the batching redesign happened, and the lockout was still active (30+ minutes, multiple re-checks) at the end of the session, so the redesigned `fetch_batch()`/`attribute()` logic has only been validated offline (unit tests on synthetic titles, not real GDELT responses). Do a full `python3 scrapers/gdelt_scraper.py` run before trusting `gdelt_news_cache.json`, and specifically watch the first batch's per-company counts for signs GDELT is struggling with query complexity (see §10's Next recommended task for what that would look like) — lower `BATCH_SIZE` if so, the timeout budget has room down to `BATCH_SIZE=3` at 150 companies.
- No automated tests exist. All verification has been manual (run scripts, inspect counts, `node --check` on extracted JS).
- Original `Itaú.jpeg` (398KB, 3840×3840) is committed to the repo even though only the small processed `assets/itau_logo.png` is used by the build — kept intentionally as a reprocessing source, not dead weight to clean up.

### Next recommended tasks
- Run `python3 scrapers/gdelt_scraper.py` end-to-end (fresh, not right after other GDELT testing) to confirm the full 10-company loop and cache output before it's trusted in a real build, then `python3 core/generate_dashboard.py` to bake it in and eyeball the News page for GDELT-origin cards.
- Get Carlo's visual sign-off on the reorg (see above) before anything else — this changed the whole navigation model of a document his team opens directly.
- Still outstanding from the source-whitelist work: `scraper.py` and `credit_scraper.py` never got the per-keyword-query redesign that fixed `brazil_scraper.py`'s relevance-dilution problem — GDELT complements this but doesn't replace it. Worth doing if GDELT alone doesn't close the gap for thin companies/sectors.
- See `IDEAS.md` for the full prioritized backlog. Tier 1 item #1 (importance badges) is done; item #2 (Data Freshness/Staleness) was proposed and **rejected by Carlo — no perceived value**, don't re-suggest it. Next candidates: #3 ("What's New Since Previous Update"), #4 (Analyst Notes), #5 (Macro Calendar Importance) — awaiting Carlo's pick.
- Otherwise: wait for feedback from Carlo/his team once the dashboard is actually circulated, rather than building further ahead of confirmed need.

---

## 8. Known Constraints

- **BCB SGS series can silently 502** — confirmed with series 12511 (returned 502/timeout on every attempt while other series responded in <1s). Before wiring up a new BCB series, sanity-check it responds with a minimal `ultimos/1` query before trusting it in the pipeline.
- **ANBIMA's free ETTJ download only retains a few months of history** — `CZ-down.asp?Dt_Ref=DD/MM/YYYY` returns real data for roughly the last ~3-4 months and empty responses further back (confirmed by testing: mid-March 2026 onward worked, January/2025 dates didn't, from a "today" of July 2026). This is why `fetch_br10y()` accumulates its own history file rather than trying to bulk-backfill — there's nothing further back to backfill from, and it would be needlessly hammering ANBIMA's server to try.
- **FOEX pulp/paper index (PIX BHKP/NBSK) is paywalled** — no free public source exists. The Cockpit no longer shows a pulp/paper metric at all (the Suzano/Klabin stock-price proxy was removed at Carlo's request); if a real feed is ever licensed, a genuine pulp/paper commodity card could be added back.
- **Jet Fuel has no clean free outright futures price** — Yahoo Finance's `JET=F` is a differential/spread contract (oscillates near zero, sometimes negative), not an outright price; no other free source was found. The Cockpit's Jet Fuel card is an intentional, honestly-labeled N/A, not a bug — don't "fix" it by wiring up `JET=F` as if it were a real price.
- **Nickel has no Yahoo Finance ticker** — confirmed dead: `LNI=F`, `NI=F`, `NID=F`, `NICKEL` all 404/delisted. FRED's `PNICKUSDM` (monthly) is the only free source found; the Cockpit's Nickel card is intentionally lower-frequency than its neighbors, labeled `"FRED (monthly)"` rather than hidden.
- **Google News RSS is not an official API** — no SLA, no guaranteed rate limits, prone to transient timeouts especially over corporate networks. Retry logic mitigates but does not eliminate this.
- **GDELT's rate limit is real and its lockout outlasts its own stated window** — the DOC 2.0 API's 429 response says "please limit requests to one every 5 seconds," but a short burst of unspaced test requests (during development, diagnosing this exact limit) triggered a lockout still active 30+ minutes later, confirmed via multiple spaced-out re-checks, not a single data point. Treat any 429 from GDELT as a signal to back off for a long time and stop, not to retry on a short timer — `gdelt_scraper.py`'s `gdelt_request()` already does this (one retry after 60s, then a hard abort for the rest of the run). Don't loosen this without a specific reason; a shorter/more aggressive retry policy risks a longer lockout on the next run too.
- **`dashboard.html` must remain a single self-contained file** — no external asset references, no `fetch()` calls. Any new feature that would require a live network call from the browser breaks the core distribution model (email/SharePoint a file, open with no server).
- **Corporate network intercepts HTTPS** — `verify=False` is required for the pipeline to run at all from the Itaú work machine. This is a known, accepted risk in this specific context (internal tool, non-sensitive public data sources only) — do not treat it as an oversight to silently "fix."
- **No test suite** — changes should be manually verified by actually running the affected script(s) and inspecting real output before considering a fix complete.

---

## 9. Project Rules

- **Never reintroduce a live server or a "Refresh" button in the UI** — the distribution model is generate-once, share-the-file. This was deliberately removed; don't add it back without explicit direction.
- **Never commit `dashboard.html` or `data/*.json`** — both are gitignored generated output. Only `data/portfolio.xlsx` (source data) is tracked.
- **Never relabel a data source as something it isn't** — if a metric can't be reliably fetched (e.g. Brazil 10Y), replace it with a real, correctly-labeled alternative rather than silently substituting different data under the original label.
- **Keep every chart's interaction pattern identical across pages** — PNG/CSV/Expand icon buttons, shared modal, `displayModeBar:false`. Don't introduce a page-specific chart UI variant without updating this file and reconciling the others.
- **Any new HTTP call must use `core.net_utils.get_with_retry()`**, not raw `requests.get()` — **except** where the target API has its own documented rate-limit contract requiring bespoke handling (see `gdelt_scraper.py`'s `gdelt_request()`: GDELT returns HTTP 429 with an explicit "one request every 5 seconds" message, which needs a long, deliberate backoff and a hard stop after one retry, not `get_with_retry`'s generic Timeout/ConnectionError-only retry loop, which doesn't inspect status codes at all and would either miss the 429 entirely or retry it too aggressively).
- **Any new age/freshness filter on news articles must be checked in every layer it exists** — this codebase has been bitten once by a filter existing in both the scraper and the renderer with different values.
- **Don't add `certifi`-based cert verification back** without confirming with Carlo — it breaks on the corporate network.
- **Don't reintroduce per-source pages** (a standalone "Brazil page," "China page," etc.) — the dashboard is organized by content type (News / Economic Data / Calendar / Cockpit / Other Datasets), each filterable by country/sector/company internally. This was a deliberate, explicitly-requested reorg away from per-source pages; don't revert to the old pattern without Carlo asking for it again.
- **Never test against GDELT with rapid/unspaced requests** — its rate-limit lockout has been confirmed to outlast its own stated "5 seconds" window by 30+ minutes. When diagnosing or extending `gdelt_scraper.py`, always throttle manually (6s+ between calls) even in one-off diagnostic scripts, not just in the production code path — a "quick test" burst can lock out the *next* real run too.
- **GDELT-sourced articles must pass the same domain-whitelist discipline as Google-News-sourced ones** — GDELT does no relevance ranking or source-quality filtering of its own (unlike Google News, which at least ranks by relevance), so skipping the whitelist for a GDELT-based feature would let through meaningfully lower-quality sources than the rest of the dashboard.
- **`scraper.py`/`brazil_scraper.py`/`credit_scraper.py` only keep articles from `core.trusted_sources.TRUSTED_DOMAINS`** — this is a hard filter checked against `entry.source.href`, not a scoring bonus; an off-whitelist article must be discarded, never merely down-scored. If Carlo asks to add/remove an outlet, edit `TRUSTED_DOMAINS` in `core/trusted_sources.py` once — don't touch the 3 scrapers individually, and don't apply this whitelist to `china_scraper.py` (it's Brazil-only and would zero out China's feed).

---

## 10. Session Handoff

### Last completed task
This session added two pieces of work on top of the prior session's 3 (Brazil 10Y/ANBIMA fix, Brazil scraper same-day redesign, 5-page reorg — all still current):

4. **Source whitelist as a hard filter**: Carlo supplied a ~130-outlet table (Brazilian financial/business/trade press, wires, government) and said unreliable sources needed to be excluded, not just down-ranked. Built `core/trusted_sources.py` (new shared module — `TRUSTED_DOMAINS` + `is_trusted(href)`), checked against `entry.source.href`. Wired as a hard pre-filter into the 3 Brazil-market scrapers (`scraper.py`, `brazil_scraper.py`, `credit_scraper.py`). See §4 for the full writeup.
5. **GDELT as a 2nd portfolio-news source**: Carlo pushed back that Bradesco (a major bank) returning 0 articles couldn't be right, and asked for a real conversation about better scraping methods and whether anything on GitHub beats Google News. Root-caused Bradesco live (not assumed): Google's 100-result cap for that query had only 7 same-week articles and only 1 trusted one, which then failed `MIN_SCORE` because the headline didn't say "Bradesco" — the *exact* relevance-dilution bug already fixed in `brazil_scraper.py`, never applied to `scraper.py`. Recommended the real fix (per-keyword queries, still outstanding) but built what was actually asked: `gdelt_scraper.py`, a 2nd independent source via GDELT's DOC 2.0 API, PT+EN, scoped to Portfolio companies, whitelist-filtered on both language legs (new `TRUSTED_DOMAINS_EN` for the English leg).
6. **Redesigned `gdelt_scraper.py` for a ~150-company portfolio before its first real run**: Carlo mentioned the final company list will have 150 entries, not 10 — at the original 1-request-per-company-per-leg design that's ~30 minutes, past `update_dashboard.py`'s 600s timeout, and the cache was only written once at the end so a timeout kill would've lost the entire run. Asked Carlo two clarifying questions (no tickers in the 150-list; batch companies per query rather than extend the timeout or rotate coverage across runs) and rebuilt around both answers: batched OR-queries (`BATCH_SIZE=5`, unverified against GDELT's real complexity ceiling — see §8), an optional `Ticker` column on `portfolio.xlsx` replacing the hardcoded `AMBIGUOUS_TERM` dict, dual-pattern (ticker + company name) attribution for batch disambiguation, and incremental checkpoint writes after every batch. See §4 for the full writeup.
7. **News page: multi-select Source filter**: Carlo wanted to filter headlines by media outlet, selecting several at once (e.g. CNN + Valor). Added `news_sources` to `load_unified_news()` (174 distinct outlets — China's feed isn't whitelisted, so it's the long tail) and a checkbox-list filter with its own search-within-filter, since 174 items is too many for a plain list. See §4.
8. **Cockpit: removed the Suzano/Klabin pulp proxies, added 9 more commodities, added a source-attribution tooltip to every Cockpit metric**: Carlo asked to drop the old proxy cards and add Natural Gas/Diesel/Jet Fuel/Gasoline/Gold/Silver/Copper/Aluminum/Nickel, plus visible sourcing for all of them *and* every other Cockpit card. Live-verified every new ticker before wiring it in — this caught two real problems: Jet Fuel's only candidate ticker (`JET=F`) turned out to be a spread contract that goes negative, not an outright price (now an honest N/A, not a fake number), and Nickel has no Yahoo Finance ticker at all (FRED's monthly series is the only free option, labeled as such). See §4 for the full writeup, including which tickers were verified real vs. rejected.
9. **6-bug fix round, filed together after Carlo actually reviewed the live dashboard**: source tooltip not displaying (icon dot was too small to see, hover-only tooltip also a poor fit for a statically-reviewed document — replaced with an always-visible text label); Jet Fuel and Nickel "broken" (Jet Fuel removed entirely — no real fix exists; Nickel's FRED fetch was genuinely broken, not flaky — root-caused to `get_with_retry`'s browser-spoofed User-Agent making FRED's endpoint hang to timeout deterministically, fixed with an explicit non-browser UA); Commodities grid "cramped" (wider cards, bigger gaps, one fewer icon button freeing up header space); FX pairs "inconsistent" (found and fixed one real backwards label, `USDBRL=X` was keyed/labeled as if it were BRL→USD when it's actually USD→BRL); China dataset "broken since the refactor" (root-caused to a single dangling `getElementById('cnCatalogBadge')` reference in `buildCatalog()` — that element was deleted during the 5-page reorg, `null.textContent = ...` was throwing and silently killing the entire Other Datasets page; the chinadata.live API itself was never broken); footer attribution wrong on 4 of 5 pages (made dynamic per page instead of one static blanket claim). See §4 for the full root-cause writeup on each.
10. **News Clipping feature**: interns were manually copying articles into Excel by hand to share with the team; Carlo wanted a "Generate Clipping" button, multi-select, and a real Excel export (Date/Title/hyperlinked Link), with as few clicks as possible. Added SheetJS via CDN (2nd frontend library alongside Plotly, no Python dependency) — click "Generate Clipping" once, click each article once to select, click "Export Excel" once, `.xlsx` downloads immediately with no confirmation dialog in between. See §4 for the full writeup, including how the export logic was verified against a real generated-and-reopened `.xlsx` file, not just JS syntax.
11. **PROTOTYPE — Chart → PowerPoint export**, integrated from a teammate's separate project (`distribuicao-combustiveis-completo/`, now a subfolder here). Studied that project's chart/PPTX architecture (Chart.js + PptxGenJS, both CDN-loaded, no server — same pattern this project already uses for Plotly/SheetJS), then added a 4th icon button (matching PNG/CSV/Expand) to every chart card on Cockpit and Economic Data that exports that one chart to a native, editable PowerPoint slide — styled like the teammate's slides (light-gray background, thin divider under the title, Calibri, top-right source line) but using each chart's own existing color rather than their orange brand palette, so it matches *this* dashboard, not theirs. **Explicitly a prototype per Carlo's instruction — not committed.** See §4 for the full writeup, including what was verified (a real generated `.pptx`, unzipped and checked at the XML level) vs. what still needs a human look (never opened in actual PowerPoint).

### Files modified (this session)
- `core/trusted_sources.py` — `TRUSTED_DOMAINS` (~130 Brazilian domains) + new `TRUSTED_DOMAINS_EN` (~14 international wires) + new `DOMAIN_TO_OUTLET` reverse lookup + `outlet_name()` + `domain_of()` hardened to accept bare hostnames (GDELT's `domain` field has no scheme, unlike Google News RSS's full `href`) + `is_trusted()` given an optional `domains` param (defaults to `TRUSTED_DOMAINS`, so all 3 existing call sites are unchanged).
- `scrapers/scraper.py`, `scrapers/brazil_scraper.py`, `scrapers/credit_scraper.py` — hard domain-filter gate wired in (source-whitelist work, item 4).
- `data/portfolio.xlsx` — added an optional `Ticker` column (blank-safe), populated for the current 10 companies (e.g. `BBDC4` for Bradesco, `VALE3` for Vale).
- `scrapers/gdelt_scraper.py` — new file, then substantially redesigned same session (item 6). Final shape: `fetch_batch()` (not `fetch_company()`/`fetch_leg()` — those were the pre-redesign, single-company versions and no longer exist) takes a batch of `{company, sector, term}` dicts, builds one OR'd query per language leg, and `attribute()` maps each returned article back to exactly one company by checking both its query term and its plain name as whole-word title matches, dropping anything that matches zero or more than one. `main()` writes `gdelt_news_cache.json` after every batch via `checkpoint()`, not just once at the end. Own throttle/backoff/429-handling (`gdelt_request()`) unchanged from the original design.
- `core/generate_dashboard.py` — added `GDELT_NEWS_FILE` constant; `load_unified_news()` now also loads and merges gdelt rows (`origin="gdelt"`, `country="Brazil"`) alongside the other 4 sources. Unaffected by the item-6 redesign — `gdelt_news_cache.json`'s shape didn't change, only how it's produced.
- `update_dashboard.py` — added a "Portfolio news (GDELT)" step right after "Portfolio news" in `STEPS`.
- `templates/dashboard_template.html` — News page: new Source filter-group (checkbox list + search-within-filter), `data-source` attribute on news cards, updated filter JS. Cockpit page: `ICON_INFO` macro added then removed same session (see item 9); Commodities grid changed to `auto-fill`/wrap then widened further; old Suzano/Klabin footnote removed. Item 9 on top: `.mkt-card-source` always-visible label replacing the icon tooltip, `cnCatalogBadge` dangling reference removed from `buildCatalog()`, `FOOTER_ATTRIBUTION` object + dynamic footer wired into `showPage()`. Item 10 on top: SheetJS CDN `<script>` tag; News feed-header gained `#clipStartBtn`/`#clipActive`/`#clipExportBtn` controls; every `.news-card` gained a `.clip-check` checkbox (hidden unless `#newsFeed.clip-mode`); new capture-phase click-interception IIFE (`clipToggle`/`clipCancel`/`clipExport` on `window`).
- `core/generate_dashboard.py` — `load_unified_news()` now also returns `news_sources`; `render()`/`main()` updated to pass it through. `_ck_make()` gained a `source` param (default `"Yahoo Finance"`). `fetch_cockpit_data()`: removed Suzano/Klabin, added Natural Gas/Diesel/Gasoline/Gold/Silver/Copper/Aluminum/Nickel (item 8), then Jet Fuel removed and the `usd_brl`/`usd_cny`/`brl_cny`/`eur_brl` FX keys/labels fixed (item 9). `_ck_fred()` now passes an explicit non-browser User-Agent instead of inheriting `get_with_retry`'s default (item 9's Nickel fix). `COCKPIT_GROUPS` updated accordingly (18 keys total, down from the 19 item 8 shipped with). Not touched by item 10 — News Clipping is 100% client-side, no Python changes.
- `templates/dashboard_template.html` (item 11, prototype) — PptxGenJS CDN `<script>` tag; new `ICON_PPTX`/`ICON_PPTX_SVG` macro; new `exportChartToPPTX(btn, ref)` + helper `_pptxChartInfo(ref)`, inserted just before the existing "Shared chart utilities" section; a PPTX icon button added to Cockpit's `.mkt-card-actions` (14 chart cards) and to Economic Data's `chart-actions` template literal inside `buildEconCharts()`; `buildEconCharts()` now also assigns `window._econCombined = combined` so the export function can look up any Economic Data chart's data/color/source by id from outside that function.
- `CLAUDE.md` — this update.

### Current state
Source-whitelist work (item 4) is fully verified — all 3 scrapers ran live post-purge, `dashboard.html` regenerated clean. **GDELT work (items 5–6) is implemented and reviewed, but has never had a full live run**: the original single-company design was validated against one real live GDELT call (before the redesign) and correctly reproduced the Bradesco findings, but GDELT's rate limit was still confirmed locked out (30+ minutes, multiple spaced re-checks) at the end of that session, so the *redesigned*, batched version has only been checked via offline unit tests — all passing — plus a full `py_compile` and a clean `generate_dashboard.py` run with no `gdelt_news_cache.json` on disk yet. **No batched GDELT query has ever actually been sent** — `BATCH_SIZE=5` is a reasoned guess, not a measured one; this is still true and unchanged this round. **Items 7–9 (Source filter, Cockpit commodities, and the 6-bug fix round) are all fully verified**: `dashboard.html` regenerated clean after every fix, structural checks confirmed all 6 issues resolved. **Item 10 (News Clipping) is fully verified, including the actual generated file**: `dashboard.html` regenerated clean, all 498 news cards confirmed to carry exactly one `.clip-checkbox` each, JS extracted and `node --check`-clean, and — going a level deeper than a syntax check — downloaded the real pinned SheetJS bundle and ran the exact export logic against synthetic article data in Node, producing a real `.xlsx`, then read it back and confirmed the header row, descending date sort, DD/MM/YYYY formatting, and genuine `External`-type hyperlink relationships on every Link cell. **Item 11 (Chart → PowerPoint, prototype) is implemented and file-level verified, but NOT committed and NOT opened in real PowerPoint**: `dashboard.html` regenerated clean, 14 Cockpit PPTX buttons confirmed (matches the 14 chart-type cards exactly), the Economic Data button confirmed present in the `buildEconCharts()` template literal (populated for all ~30 charts at runtime, so 1 in the static source is correct), JS extracted and `node --check`-clean. Went a level deeper: since the CDN "full bundle" is browser-oriented and can't `require()` in plain Node (same issue as SheetJS), installed the npm-published `pptxgenjs` package in the scratchpad, ran the actual `exportChartToPPTX` logic against realistic fake chart data, wrote a real `.pptx`, unzipped it, and confirmed via the raw XML that the title/source/footer text and the chart's real data points and date labels all landed correctly in a genuine native chart object — not a screenshot. `git status` confirms nothing from item 11 (or anything else this session) has been staged or committed.

### Next recommended task
Run `python3 scrapers/gdelt_scraper.py` fresh once GDELT's lockout clears (unknown exactly when — it already outlasted every re-check across two sessions) to get the first real `gdelt_news_cache.json`, and **watch the first run's per-company article counts closely** — since `BATCH_SIZE` was never verified, a company getting suspiciously few (or zero) results relative to others in its batch, or every company in a batch getting results skewed toward whichever term came first in the OR clause, would indicate GDELT is silently struggling with the batch's complexity and `BATCH_SIZE` should be lowered (try 3, worst case still fits the timeout — see §4's math). Then `python3 core/generate_dashboard.py` and check the News page for GDELT-origin cards. Also get Carlo's visual/hands-on sign-off on News Clipping and the Chart→PowerPoint prototype (click through both in a browser, and for the PPTX prototype specifically, actually open a downloaded file in real PowerPoint — Node-level XML verification confirms the file is structurally correct but not that fonts/spacing/axis-label-crowding look right) and on the 6-bug-fix round from two turns back, especially the Commodities grid spacing, which was a subjective "cramped" call this environment can't fully judge without rendering a browser. Once the PPTX prototype is confirmed good, it still needs an explicit "commit this" from Carlo — it's deliberately uncommitted per his instruction. After that, same as before: the still-unreviewed 5-page reorg, and the per-keyword-query redesign for `scraper.py`/`credit_scraper.py` remains on the table if GDELT alone doesn't close the coverage gap.

### Notes for future Claude
- This file (§1–9) is the durable context. Read it fully before touching code. `IDEAS.md` is the backlog — check it before proposing new features so you don't re-litigate a prioritization decision Carlo already made.
- The project has gone through several rounds of "user reports X is broken/inconsistent → root-cause it, don't just patch the symptom" — e.g. the news-age bug existed in 5 places, not 1; the BCB 12511 issue was confirmed via direct curl testing to be BCB's fault; the Brazil scraper's "not enough articles" issue and later the Bradesco/GDELT issue were both confirmed via direct live testing to be Google News' relevance-ranking behavior, not a scoring bug — twice now, same root cause, two different scrapers. Keep that standard: verify root cause with actual tool calls (curl, run the script, inspect real output) before claiming a fix, and check whether a "new" bug is actually an old, already-diagnosed one that just wasn't fixed everywhere.
- Carlo is non-error-tolerant about financial data accuracy — when a data source is broken or wrong, prefer "clearly labeled fallback" or "honest N/A" over "quietly wrong number," and don't stop at the first plausible-looking fix if he says it's still not right (Brazil 10Y took three iterations: broken BCB series → Selic, which he rejected as the wrong metric entirely → the real ANBIMA yield curve).
- The user has been iterating rapidly on UI/UX feedback in short messages, and separately has asked for big-picture planning before coding when the change is large (used `EnterPlanMode` for the 5-page reorg, asked clarifying questions on the ambiguous parts, got explicit sign-off on the plan before writing any code). Match the weight of the response to the weight of the ask — quick fixes get quick fixes, "this is a big reorganization" gets a real plan.
- When Carlo asks for discussion/planning before coding, respect that explicitly — produce the artifact requested, give a direct opinion when asked, but don't start implementing until he confirms scope. When he then says "let's have a conversation" about a technical problem, that's an invitation for a grounded diagnosis and real tradeoffs, not a request to immediately start coding — but once he says "try to implement X," that's the green light.
- When Carlo says "restrict to X" or "only use X," read it as a hard filter, not a scoring nudge — this project already had a `TRUSTED_SOURCES` scoring bonus in place when he asked for a source whitelist, and the existing mechanism was NOT what he meant; it had to become an exclusionary gate. Don't assume an existing similar-looking mechanism already satisfies a new, more absolute-sounding instruction. The same "be considerate about sources" instruction, applied to GDELT, meant: still enforce the domain whitelist (GDELT does no quality filtering of its own, so this mattered even more than for Google News), and respect the external API's own rate-limit contract rather than just the local dashboard's data-quality bar — "considerate" cut both ways, toward what's retrieved and toward the API being called.
- Live-testing an external API to confirm its real behavior (not just its docs) is this project's established standard (see the BCB 12511 and Google News relevance-ranking precedents) — but it has a cost when the API itself has a strict, easily-tripped rate limit like GDELT's. Budget for that: test the minimum necessary to confirm syntax/schema, then stop and let real production traffic (which won't have the same burst pattern) be the actual end-to-end validation, rather than repeatedly re-testing into a worsening lockout.
- A ticker "returning data" isn't the same as it being the *right* data — `JET=F` downloaded cleanly and looked like a normal yfinance response, but a single glance at the actual value (negative) was the tell that something was wrong; pulling the full history confirmed it was a spread/differential contract, not an outright price. When adding any new market-data ticker, sanity-check the *value*, not just whether the fetch succeeded — `yf.Ticker(x).info`'s `shortName`/`quoteType`/`exchange` fields are also worth checking (this is how `ALI=F` was confirmed as real Aluminum futures rather than assumed).
- **When a timeout bump doesn't fix a timeout, the timeout was never the actual problem** — Nickel's FRED fetch was first diagnosed as "occasional cold-start latency" and given a 15s→25s bump, which didn't help (still failed, ~58s across 2 attempts). The real cause was `get_with_retry`'s browser-spoofed `DEFAULT_HEADERS` User-Agent — added specifically to get past Google News' bot detection — causing FRED's endpoint to hang deterministically, confirmed by testing the identical URL with and without that header. **`core.net_utils.DEFAULT_HEADERS` is tuned for Google News, not a universal-safe default** — before assuming a new HTTP call site can just use `get_with_retry()` with no `headers=` override, consider whether the browser-spoof UA could cause *this specific* target server to behave worse, not better. When a fetch fails inconsistently or times out for reasons that don't obviously map to network conditions, isolate variables one at a time (headers, timeout, retry count) with direct `requests` calls before accepting "flaky external service" as the diagnosis — that was the right call for GDELT's rate limit (a real, confirmed, documented constraint) but the wrong one here.
- **This project's "verify root cause with actual tool calls" standard extends to the frontend, not just external APIs** — the "China dataset broken" report turned out to have nothing to do with chinadata.live (both fetch functions were re-verified live and worked perfectly); it was a single dangling `getElementById()` reference left over from the reorg deleting the element it pointed to. A quick script cross-referencing every `getElementById()` call against every `id="..."` actually defined in the template (see §4) found it in seconds and would be worth re-running after any future template restructuring — this class of bug (function silently dies partway through because of a null-property-access) produces exactly the symptom Carlo reported ("no longer working") with zero error visible to an end user, since there's no server log to check on a static-file dashboard.
- **For a browser-generated downloadable file (News Clipping's `.xlsx`), `node --check`-clean JS syntax is not sufficient verification** — it proves the code parses, not that the *output file* is correct. The higher-value check: actually run the export logic against real/synthetic data with the real library (downloaded the pinned SheetJS bundle rather than trusting the CDN reference blindly), write the file, then *read it back* and assert on its contents — this is what caught nothing in this case (the logic was right first try), but it's the check that would have caught a wrong cell reference, a bad date format, or a hyperlink silently not attaching. Worth the extra few minutes whenever a feature's entire value proposition is "produces a correct file," since a syntax-clean function that writes a subtly-wrong file is a worse failure mode than one that visibly crashes.
